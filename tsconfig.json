// This file is kept only for the IDEs to resolve the paths correctly.
// The actual compilation is done by SWC, which uses the .swcrc file.
{
  "compilerOptions": {
    "baseUrl": "./",
    "paths": {
      "@lib/*": ["src/*"],
      "@test/*": ["test/*"],
    }
  },
  "exclude": ["node_modules", "dist"]
}